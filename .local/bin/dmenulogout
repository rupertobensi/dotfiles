#!/bin/bash

RET=$(echo -e "shutdown\nreboot\nlogout\ncancel" | dmenu -l 5 -h "22" -fn "monospace-11" -sb "#9770ae" -p "Logout")

case $RET in
	shutdown) sudo shutdown -h now & ;;
	reboot) sudo reboot ;;
	logout) i3-msg exit & ;;
	*) ;;
esac
